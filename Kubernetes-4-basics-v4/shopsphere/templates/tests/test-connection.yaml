apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shopsphere.fullname" . }}-test-connection"
  labels:
    {{- include "shopsphere.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shopsphere.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
