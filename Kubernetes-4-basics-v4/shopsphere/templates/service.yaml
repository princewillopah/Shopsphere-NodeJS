apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopsphere.fullname" . }}
  labels:
    {{- include "shopsphere.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopsphere.selectorLabels" . | nindent 4 }}
