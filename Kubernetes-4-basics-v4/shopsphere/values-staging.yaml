api:
  replicaCount: 1
  image:
    tag: staging
  resources:
    requests: { cpu: 100m, memory: 128Mi }
    limits: { cpu: 500m, memory: 512Mi }
  initContainer:
    image: "mysql:8.0"
    resources:
      requests:
        cpu: "20m"
        memory: "32Mi"
      limits:
        cpu: "50m"
        memory: "64Mi"

ui:
  replicaCount: 1
  image:
    tag: staging
ingress:
  host: staging.shopsphere.com